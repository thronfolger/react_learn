// Индекс массива соответствует числу
const initState = [
  "Zero",
  "One",
  "Two",
  "Three",
  "Four",
  "Five",
]

const numberReducer = (state = initState) => {

    return state;

};

export default numberReducer;
